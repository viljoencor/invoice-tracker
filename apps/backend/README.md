# Invoice Tracker Backend

FastAPI backend for invoice management system.

## Install

```bash
uv sync
```

## Run

```bash
uv run uvicorn app.main:app --reload
```

## Test

```bash
uv run pytest
```
